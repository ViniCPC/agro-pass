# =============================================================
# PostgreSQL — usado pelo docker-compose.yml
# Copie para .env e ajuste os valores antes de rodar
# =============================================================
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
